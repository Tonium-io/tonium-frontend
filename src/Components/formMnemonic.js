import React from 'react';

const formMnemonic = ({}) => {
  return (
    <form autoComplete="off">
      <TextField id="standard-basic" label="Standard" />
    </form>
  );
};

export default formMnemonic;
